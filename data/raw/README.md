# Raw Dataset

Place the public/authorized reference dataset here:

`DataCoSupplyChainDataset.csv`

The raw CSV is not bundled with this repository so the repository remains lightweight and respects source/distribution requirements.
