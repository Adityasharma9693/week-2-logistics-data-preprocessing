# Processed Data

The Python preprocessing script writes the cleaned dataset to this folder as:

`cleaned_logistics_data.csv`

This file is generated after the raw dataset is placed in `data/raw/` and the preprocessing script is run.
